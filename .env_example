# -----------------------------------------------------------------------------
# Rename this file to `.env` (or copy the contents) and fill in the values.
# All values are read at runtime via python-dotenv. Leave optional fields blank
# if you don't use that integration.
# -----------------------------------------------------------------------------

# ------------------------ Google Drive / Sync -------------------------------
FOLDER_ID=your_google_drive_folder_id

# ------------------------ Gemini / Google AI --------------------------------
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-1.5-flash

# ------------------------ Supabase Vector Store -----------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_TABLE=page_summaries
SUPABASE_QUERY_FN=match_page_summaries
SUPABASE_MATCH_THRESHOLD=0.0

# ------------------------ Embeddings / Hugging Face ------------------------
EMBEDDING_MODEL=google/gemma-2-2b-it
HUGGINGFACE_HUB_TOKEN=your_optional_hf_token
HF_TOKEN=your_optional_secondary_hf_token
BATCH_SIZE=32

# ------------------------ Retrieval / RAG ----------------------------------
RETRIEVER_TOP_K=2



